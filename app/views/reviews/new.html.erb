<h2>Add a review</h2>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :rating, collection: (0..5).to_a, prompt: "Select your rating" %>
  <%= f.input :content %>
  <%= f.button :submit %>
<% end %>
